specVersion: "v1"
serviceId: "userwebapp"
location: "asia-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "userwebapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
